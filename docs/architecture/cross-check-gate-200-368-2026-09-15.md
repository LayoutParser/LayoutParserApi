# Cross-check do gate LayoutParserReact#200 (issue #368) × estado atual da API

Data: 2026-09-15
Autor: @lp-architect (Aria)
Fonte: tabela original do gate #200 (LayoutParserReact), reaberta como epic guarda-chuva #368.
Base: `develop` (após merge de #379/#380/#381/#367/#377/#378/#340/#341/#345/#346/#355/#356/#322).

> **Escopo:** análise de delta, nenhum código novo. Legenda: **JÁ EXISTE** = disponível hoje, com
> ponteiro exato · **PARCIAL** = existe base, falta parte · **NÃO EXISTE** = capacidade nova.
> Não repete o cruzamento já feito em
> [`cross-check-contratos-226-198-react-2026-09-10.md`](cross-check-contratos-226-198-react-2026-09-10.md)
> — só referencia o que aquele documento já havia decidido (198.1/198.2/198.4 nasceram dele e já
> foram implementados desde então, ver evidência abaixo).

---

## Tabela reavaliada

| # | Item da tabela original | Veredito atual | Evidência |
|---|---|---|---|
| 1 | `GET /api/workspaces/me` | **JÁ EXISTE — completo** | `Controllers/WorkspacesController.cs:38-68`. Retorna `activeWorkspaceId` + `workspaces[]` (`workspaceId`/`name`/`kind`/`role`/`createdAt`), cria workspace pessoal idempotente na primeira chamada, `401` explícito sem identidade resolvida, `503` fail-closed se o SQL cair. Cobre 100% do contrato cross-repo §2 (comentário na linha 9 referencia o doc). Nada faltando — sem paginação porque a lista de workspaces de um usuário não tem volume que justifique. |
| 2 | Pacote fiscal versionado | **JÁ EXISTE — sem mudança de escopo desde 09-10** | `Controllers/FiscalMappingPackagesController.cs` — pacote + revisões + inventário Excel, confirmado no cross-check anterior. Ganhou vizinhança (`FiscalProfile`, #379) mas o contrato do pacote em si não mudou. |
| 3 | `MappingDraft` | **JÁ EXISTE — bem mais completo que em 09-10** | `Controllers/MappingDraftsController.cs`: CRUD de draft (`POST .../drafts`, `GET .../mapping-drafts/{id}`), sugestões assíncronas (`POST/GET .../suggestions`), edição de regra com concorrência otimista (`PATCH .../rules/{ruleId}`, ETag/412/428). Desde 09-10 ganhou: `FiscalProfile` anexado (#379), editor de artefato manual pós-compile (#381), filtros de listagem de release (#377, ver item 198.4 abaixo), arquivamento de release (#378, ver item 198.1). O núcleo de draft em si (regras estruturadas, não texto) não mudou de modelo — só ganhou vizinhança rica. |
| 4 | Geração de TCL/XSL/XSLT (contrato de saída) | **JÁ EXISTE — contrato estável exposto** | `Controllers/MappingCompilationController.cs:73` (`POST .../compile`, assíncrono via job) → `MappingDraftRuleTranspiler`/`DeterministicXslTranspiler` (determinístico, sem LLM na hora H). Artefato compilado tem `Hash` (`MappingReleaseArtifact.Hash`, base para ETag/`baseArtifactHash`), status de ciclo de vida (`MappingReleaseStatus`), e desde #381 um caminho formal de edição manual pós-compile com `ArtifactSource` rastreado. O gap que a tabela original apontava ("contrato estável") está fechado — schema de artefato, hash e imutabilidade pós-`publish` são formalizados desde a fundação fiscal (Slices 1-7) e reforçados por #381. |
| 5 | `MappingExplanation` | **JÁ EXISTE — capacidade nova desde 09-10, agora completa** | `Controllers/MappingExplanationController.cs` (Slice 4, issues #226/#227), rota `GET api/workspaces/{workspaceId}/mappings/{mappingId}/versions/{version}/explanation`. Resolve `engine` a partir de `mappingId` (tenta `draftId` primeiro, cai para `MapperGuid` Sysmiddle), delega a um de 3 adapters (`IMappingExplanationAdapter`: tcl/xslt/sysmiddle — `Services/Fiscal/{Tcl,Xslt,Sysmiddle}ExplanationAdapter.cs`). Deliberadamente **sem** `MappingEngineGuardFilter` — explicar Sysmiddle é o caso permitido (spec §4: Sysmiddle só `explain`, nunca `author`). Fail-closed em 404 uniforme (sem membership OU nada resolve). Este item estava "NÃO EXISTE" na tabela original de agosto — hoje é o mais completo de todos, com contrato canônico independente de motor. Ver também `docs/architecture/adr-contrato-explicabilidade-fiscal-2026-09-07.md`. |
| 6 | Capabilities Sysmiddle read-only | **JÁ EXISTE — via gate de filtro, não endpoint dedicado** | Issue #232 (CLOSED). Implementado como `Services/Filters/MappingEngineGuardFilter.cs`, aplicado a toda rota mutável de Draft/compile/release — nega `engine=sysmiddle` no servidor (não só UI). `MappingExplanationController` explicitamente NÃO aplica o filtro (linha 11, comentário), coerente com "Sysmiddle só explica, nunca autora". Nota de gap: o checklist do #232 pede também `capability retorna author=false/compile=false/publish=false` como um **payload consultável** (tipo endpoint `GET .../capabilities`) — isso **não existe como endpoint dedicado**; a capability hoje só é observável indiretamente (tentando mutar e recebendo o bloqueio, ou por `MappingExplanation.engine` na resposta). Se o front precisa decidir *antes* de renderizar um botão de "editar" se a ação é permitida, sem tentar e falhar, falta um endpoint leve de capability query — candidato a sub-issue pequena (ver abaixo). |
| 7 | Test Lab (XSD/diff/cobertura/provenance) | **JÁ EXISTE — muito mais completo que em agosto, provavelmente fecha o item** | `Controllers/MappingCompilationController.cs:315` (`POST .../test-runs`, Fiscal Test Lab) já existia parcial. Desde então: diff por `ruleId` (#367, `MappingTestRunDivergence.RuleId` com provenance nó→regra→campo — `Models/Entities/Fiscal/MappingRelease.cs:51`), diff release A×release B (#380, `GET .../mapping-drafts/{draftId}/releases/diff`, linha 143 do compilation controller), cobertura de destinos obrigatórios (#380, `requiredCoverage` — conceito distinto do `CoveragePercent` de execução, conforme já formalizado no cross-check de 09-10 item 198.5). `CanonicalDiffer` continua sendo a fonte estruturada (`Kind`/`XPath`/`Expected`/`Actual`). Os dois gaps que o cross-check de 09-10 apontava como "NÃO EXISTE" (198.2b agregação por destino + diff release×release, e 198.5 cobertura de obrigatórios) foram implementados juntos na #380 — confirmado por `tests/LayoutParserApi.Tests/Controllers/MappingReleaseDiffAndCoverageControllerTests.cs`. |
| 8 | OpenAPI / contract check automatizado | **PARCIAL — geração existe, verificação de quebra de contrato em CI não existe** | `Program.cs:287` (`AddSwaggerGen`) + `Program.cs:961-962` (`UseSwagger`/`UseSwaggerUI`, rota `api/swagger/{documentName}/swagger.json`) — o documento OpenAPI é gerado e servido. **O que continua não existindo:** nenhum step de CI compara o schema OpenAPI entre PRs para detectar quebra de contrato (breaking change em rota/campo/tipo). Os workflows atuais (`pr-validate.yml`, `ci-dev.yml`, `merge-gate.yml`, `config-drift-report.yml`, `dependency-review.yml`, `gitleaks.yml`) não têm nenhum step de diff de schema — confirmado por grep (`contract`/`schema-check`/`openapi-diff` sem match em `.github/workflows/`). Capacidade nova, pequena e bem localizada: adicionar um step ao `pr-validate.yml` que gera o swagger.json do PR e compara contra o de `develop` (ferramenta gratuita candidata: `oasdiff` ou `openapi-diff`, ambos CLI livres, sem custo de licença). |

---

## Resumo executivo

### Já fecha só documentando (nenhum código novo)

- **Item 1** (`GET /api/workspaces/me`) — completo, sem gap. Só formalizar no doc do front que o
  contrato está fechado (já é o que a tabela pedia).
- **Item 2** (pacote fiscal) — sem mudança desde 09-10, nada a fazer.
- **Item 3** (`MappingDraft`) — modelo estável, só documentar a vizinhança nova (`FiscalProfile`,
  editor manual, filtros, arquivamento) que já existe.
- **Item 4** (contrato de saída TCL/XSL/XSLT) — contrato de artefato (hash, status, imutabilidade
  pós-publish) já formalizado; documentar para o front.
- **Item 5** (`MappingExplanation`) — capacidade nova completa; documentar o shape canônico e o
  fallback sysmiddle→opaque/unsupported para o time React consumir.
- **Item 7** (Test Lab) — diff por regra, diff release×release e cobertura de obrigatórios já
  existem; documentar os 3 shapes (`divergencesByRuleId`, `GET .../releases/diff`,
  `requiredCoverage`) — provavelmente fecha o item por completo, sem código novo.

### Precisa de implementação nova

| # | Item | Tamanho | Depende de decisão de produto/design |
|---|---|---|---|
| 6b | Endpoint leve de capability query (`GET .../capabilities` por `mappingId`/`engine`, devolvendo `author`/`compile`/`publish` booleanos) — hoje a capability só é observável indiretamente (tentar e falhar) | **P** | Não — é leitura pura de uma regra já implementada no filtro; só expor o mesmo booleano que o `MappingEngineGuardFilter` já decide |
| 8 | Contract check automatizado de OpenAPI em CI (diff de schema entre PR e `develop`, via `oasdiff`/`openapi-diff`) | **P** | Não — mecânico, adicionar step ao `pr-validate.yml` |

### Recomendação de quebra em sub-issues

1. **Doc-only (1 issue, @lp-doc):** "Fechar gate #200/#368 — documentar contratos já implementados
   para o time React": itens 1, 2, 3, 4, 5, 7 da tabela acima. Não bloqueia nada, é o maior volume
   de trabalho útil imediato — desbloqueia o front a consumir capacidades que já existem e que
   provavelmente estão sendo reinventadas ou aguardando no lado deles por falta de visibilidade.

2. **#6b — endpoint de capability query** (1 issue, **P**, sem dependência, @lp-backend-dev) —
   pequeno, mecânico, reaproveita a decisão que `MappingEngineGuardFilter` já toma. Só vale a pena
   se o front confirmar que precisa decidir *antes* de tentar (ex.: esconder botão de editar).
   Verificar com o time React antes de abrir — pode já estar coberto pelo fato de
   `MappingExplanation.engine` no payload já indicar Sysmiddle.

3. **#8 — contract check automatizado de OpenAPI** (1 issue, **P**, sem dependência,
   @lp-devops) — adicionar step ao `pr-validate.yml`: gerar `swagger.json` do PR, comparar contra
   `develop` com `oasdiff` (ou equivalente), falhar o gate em breaking change não anotado.

**Ordem sugerida:** 1 (doc, maior valor imediato) → 3 (mecânico, independente) → 2 (só se
confirmado com o front que é necessário).

## Nota sobre a origem da issue #368

A epic foi aberta em agosto contra uma tabela que hoje está majoritariamente desatualizada: dos 8
itens originais, **6 já estão implementados** (1, 2, 3, 4, 5, 7), **1 está parcial** (8 — geração
existe, falta o gate de CI) e **1 revela um gap pequeno não previsto na tabela original** (6b —
capability como payload consultável, distinto do gate de enforcement que já existe). O trabalho
recente em #340/#341/#345/#346/#355/#356/#367/#377/#378/#379/#380/#381/#322 fechou a maior parte
do gate sem que ninguém tivesse marcado a issue como resolvida — o item de maior valor aqui é
**documentação**, não código.
